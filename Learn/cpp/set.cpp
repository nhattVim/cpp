// [CTDL & GT]. Buổi 1. Vector, Iterator, Pair, Set Trong C++
// Set là một container cực kì mạnh mẽ trong thư viện STL của C++, Set sẽ giúp code tối ưu và ngắn gọn hơn rất nhiều
// Tính chất:   + Set là một container mà mỗi phần tử trong đó là duy nhất, tức là sẽ không có 2 phần tử có giá trị giông nhau
//              + Các phần tử trong set được sắp xếp theo thứ tự tăng dần về giá trị số và tăng dần về thứ tự từ điển nếu là xâu kí tự

#include <bits/stdc++.h>
#define fu(i,a,b) for (int i = a; i < b; i++)
#define fd(i,a,b) for (int i = a; i > b; i--)
#define sz(x) x.size()
#define f first
#define s second
#define vi vector<int>
#define rs resize
#define pb push_back
#define pii pair<int, int>
#define IOS ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

using namespace std;
using ll = long long;

int main (int argc, char *argv[]) {
    IOS;
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    

    return 0;
}
