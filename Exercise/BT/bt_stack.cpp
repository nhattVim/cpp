#include <bits/stdc++.h>

using namespace std;

int main (int argc, char *argv[]) {

    // Chuyen doi so nhi phan

    // int n; cin >> n;
    // stack<int> st;
    // while (n != 0) {
    //     st.push(n % 2);
    //     n /= 2;
    // }
    // while (!st.empty()) {
    //     cout << st.top();
    //     st.pop();
    // }

    // Dao nguoc chuoi string

    // string s;
    // getline (cin, s);
    // stringstream ss(s);
    // string word;
    // stack<string> st;
    // while (ss >> word) {
    //     st.push(word);
    // }
    // while (!st.empty()) {
    //     cout << st.top() << " ";
    //     st.pop();
    // }

    // Kiem tra dau ngoac hop le (Valid Parentheses)
    
    // string s; cin >> s;
    // stack<char> st;
    // for (char x : s) {
    //     if (x == '(') {
    //         st.push(x);
    //     } else if (x == ')') {
    //         if (st.empty()) {
    //             cout << "INVALID\n";
    //             return 0;
    //         } else {
    //             st.pop();
    //         }
    //     }
    // }
    // if (st.empty()) {
    //     cout << "VALID\n";
    // } else {
    //     cout << "INVALID\n";
    // }

    return 0;
}
