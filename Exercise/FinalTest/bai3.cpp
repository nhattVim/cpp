#include <bits/stdc++.h>
#define IOS std::ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)

using ll = long long;
using std::cin;
using std::cout;
using std::endl;

int n, m, a[105][105];

void nhap() {
    cin >> n >> m ;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            cin >> a[i][j];   
        }
    }
}

int main (int argc, char *argv[]) {

    return 0;
}
